{
  "name": "environment",
  "version": "1.0.0",
  "description": "Environment for LeadScore",
  "main": "run.js",
  "dependencies": {
    "dotenv": "^8.2.0"
  },
  "devDependencies": {
    "@types/multer": "^1.3.10"
  },
  "scripts": {
    "setup": "node run.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC"
}
